database systems workshop
Notes:
1. A user may not bid on an item he or she is also selling.

DROP TRIGGER IF EXSISTS same_bid_check;

CREATE TRIGGER 	before_insert_bid BEFORE INSERT ON Bid,
FOR EACH ROW
DELIMETER//
BEGIN
IF NEW.userID=(select userID from item where item.itemID=NEW.itemID) THEN
CALL'error0:A user cannot bid on an item they are also selling';
END IF;
//2. No auction may have two bids at the exact same time.

IF(



END;
DELIMITER//;